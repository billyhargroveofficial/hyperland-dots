# Local environment used by configured harnesses and MCP servers.
# Add only the variables required on this machine, for example:
# export SERVICE_TOKEN=""
#
# The real secrets.env is intentionally excluded from Git and should be 0600.
